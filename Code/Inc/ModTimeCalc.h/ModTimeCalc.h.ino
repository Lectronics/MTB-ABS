int wheelSlip(int time[5], int setPoint, int placeholder) {
/* This function returns modTime
time variables are for 
*/
// 1) For a sudden deceleration of the wheel & brake set, modTime should be very fast
    return 30
  }
  // placeholder here 
  if((time[0] > setPoint) AND (time[1] > setPoint) AND (time[2] > setPoint)){
    // DO NOT MODULATE
  }
    if((time[0] > setPoint) OR (time[1] > setPoint) OR (time[2] > setPoint)){
      return 10
  }
}
